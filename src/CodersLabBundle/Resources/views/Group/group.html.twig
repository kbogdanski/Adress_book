{% extends "::base.html.twig" %}

{% block title %}Grupy{% endblock %}

{% block body %}
<h1>Grupy</h1>
<div class="col-xs-1"></div>
<center>
    <div class="list-group col-xs-7">
        <a class="btn btn-ls btn-primary center-block" href="/add">Dodaj grupę</a>
        {% for group in groups %}
            <a href="/group/{{group.id}}" class="list-group-item">{{ group.title }}</a>
        {%endfor%}
    </div>
</center>
<div class="col-xs-1"></div>

{% endblock %}
